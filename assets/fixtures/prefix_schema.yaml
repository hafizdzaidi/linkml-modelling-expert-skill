id: http://example.org/prefix-example
name: prefix-example
prefixes:
  ex: http://example.org/
  xsd: http://www.w3.org/2001/XMLSchema#
  linkml: https://w3id.org/linkml/
default_prefix: ex

imports:
  - linkml:types

classes:
  Thing:
    description: "Generic thing"
    slots:
      - id
      - label

slots:
  id:
    description: "Unique identifier"
    identifier: true
    range: string
  label:
    description: "Human-readable label"
    range: string
